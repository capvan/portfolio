.section-title {
    font-size: 36px;
    font-weight: bold;
    text-align: left;

    &--small {
        font-size: 28px;
        text-align: center;
    }

    &--small__left {
        text-align: left;
    }
}